pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build Docker Image and Push to Docker Hub') {
            steps {
                script {
                    // Build the Docker image
                    sh 'docker build -t job .'

                    // Log in to Docker Hub using the pre-added credentials
                    docker.withRegistry('https://registry.hub.docker.com','4c0e2a42-f356-45d5-8376-96989f549691') {
                        
                      sh 'docker tag job sohilap/job'

                      // Push the Docker image to Docker Hub
                        sh 'docker push sohilap/job'
                    }
                }
            }
        }
    }
}
